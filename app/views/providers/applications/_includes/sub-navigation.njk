{{ appSubNavigation({
  label: "Sub navigation",
  items: [{
    text: "Details",
    href: "/providers/" + provider.code + "/applications/" + application.code,
    active: true if subNavId == "details"
  }, {
    text: "History",
    href: "/providers/" + provider.code + "/applications/" + application.code + "/history",
    active: true if subNavId == "history"
  }]
}) }}
